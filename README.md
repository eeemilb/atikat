# АТИКАТ - Динамическое приложение для отображения топиков

Телеграм WebApp для чтения файлов и папок из Supabase Storage. Приложение автоматически создает топики при добавлении новых папок и файлов в хранилище.

## 📋 Возможности

✅ **Автоматическая загрузка топиков** - топики создаются на основе структуры папок в Supabase Storage  
✅ **Трёхуровневая навигация** - сортировка по категориям и подкатегориям  
✅ **Поддержка Markdown** - автоматический парсинг .md файлов  
✅ **Профиль пользователя** - интеграция с Telegram  
✅ **Адаптивный интерфейс** - мобильный дизайн  
✅ **Полная автоматизация** - не требует редактирования БД  

## 🛠️ Начальная настройка

### 1. Подготовка Supabase Storage

1. Перейдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Создайте новый bucket с названием **`topics`** (или используйте своё имя)
3. Установите публичный доступ к bucket (Storage → Policies)

### 2. Структура папок в Storage

Создайте структуру в bucket `topics`:

```
topics/
├── электроника/
│   ├── circuitry.md
│   └── microcontrollers.md
├── проекты/
│   ├── edm-machine/
│   │   ├── overview.md
│   │   └── schematics.md
│   └── pcb-design.md
└── документация/
    └── README.md
```

**Важно:** Вложенность папок может быть произвольной - приложение поддерживает любой уровень иерархии.

### 3. Изменения в коде

В файле `index.html` (строка ~103) измените значение `STORAGE_BUCKET` на название вашего bucket:

```javascript
const STORAGE_BUCKET = 'topics'; // Ваше имя bucket
```

## 🚀 Как использовать

### Добавление нового топика

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Storage → topics**
3. Создайте папку или загрузите файл:
   - **Папки** отображаются как категории
   - **Markdown файлы** (.md) отображаются как топики с форматированием
   - **Текстовые файлы** отображаются как есть

4. Приложение автоматически загрузит новый контент при следующем открытии

### Примеры структур

#### Простая структура (плоская)
```
tопик-1.md
топик-2.md
топик-3.md
```

#### Иерархическая структура
```
Категория 1/
  Подкатегория 1/
    файл.md
  Подкатегория 2/
    файл.md
```

## 📝 Формат файлов

### Поддерживаемые форматы

- **Markdown (.md)** - рекомендуется
  - Поддерживается заголовки (`#`, `##`, `###`)
  - Жирный текст (`**текст**`)
  - Курсив (`*текст*`)
  - Код (`` `код` ``)

- **Текстовые файлы (.txt)** - отображаются как есть
- **Другие форматы** - отображаются сырым текстом

### Пример Markdown файла

```markdown
# Заголовок топика

## Подраздел

Описание с **жирным** текстом и *курсивом*.

### Подраздел 2

Код: `let x = 10;`
```

## 🔄 Синхронизация

Приложение **автоматически загружает** новые файлы и папки при каждом открытии. Если вы хотите обновить список топиков:

1. Нажмите на вкладку "Файлы" в приложении
2. Список обновится автоматически

## ⚙️ Технические детали

### Используемые технологии

- **Frontend:** HTML5 + CSS3 + JavaScript
- **Backend:** Supabase (PostgreSQL + Storage)
- **Platform:** Telegram WebApp
- **API:** Supabase JavaScript SDK v2

### Функции

| Функция | Описание |
|---------|----------|
| `loadFileStructure(path)` | Загружает список папок и файлов из Storage |
| `openFile(filePath, fileName)` | Загружает и отображает содержимое файла |
| `navigateToFolder(path)` | Переходит в подпапку |
| `parseMarkdown(md)` | Парсит Markdown в HTML |
| `goBack()` | Возвращается на уровень выше |

## 🐛 Решение проблем

### Файлы не загружаются
- Проверьте, что bucket **публичный**
- Убедитесь в правильности названия bucket в коде
- Откройте DevTools (F12) → Console и проверьте ошибки

### Markdown не форматируется
- Убедитесь, что файл имеет расширение `.md`
- Проверьте синтаксис Markdown в файле
- Парсер поддерживает базовое форматирование

### Профиль не отображается
- Откройте приложение через Telegram
- Убедитесь, что у вас установлен аватар в Telegram

## 📦 Развертывание

Приложение развёрнуто на:
- **Production:** https://atikat.vercel.app
- **GitHub Pages:** можно развернуть через Actions

## 🔐 Безопасность

⚠️ **Важно:** Хранилище публичное, не загружайте конфиденциальную информацию.

## 👨‍💻 Разработка

Для локальной разработки:

```bash
git clone https://github.com/eeemilb/atikat.git
cd atikat
# Откройте index.html в браузере
```

Отредактируйте `index.html` и сохраните - изменения применяются сразу.

## 📄 Лицензия

MIT - свободное использование в личных и коммерческих целях.

## 💬 Обратная связь

Если у вас есть вопросы или предложения, создайте Issue в GitHub.
